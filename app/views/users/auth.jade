extends ../layouts/index

block body
  h1= title

  .row    
    .span6
      if (typeof errors !== 'undefined')        
        ul
          each error in errors
            li= error.type

      block auth
